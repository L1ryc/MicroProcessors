;====================================================================
; Main.asm file generated by New Project wizard
;
; Created:   Sun Oct 12 2025
; Processor: 8086
; Compiler:  MASM32
;
; Before starting simulation set Internal Memory Size 
; in the 8086 model properties to 0x10000
;====================================================================

DATA SEGMENT
	 PORTA		EQU		500H ; input				0101	0000	0000	
	 PORTB		EQU		501H; output				0101	0000	0001
	 
DATA ENDS

CODE    SEGMENT PUBLIC 'CODE'
        ASSUME CS:CODE, DS:DATA
	
	ORG 0000H
	XOR AX,AX
	XOR BX,BX
	XOR CX,CX
	XOR DX,DX
	
START:

      CHECK_INPUT:
	      MOV DX, PORTA
	      IN AL, DX
	      
	      CMP AL, 00000001B
	      JE TOGGLE
	      
	      CMP AL, 00000010B
	      JE SNAKE
	      
	      CMP AL, 00000100B
	      JE CYCLONE
	     
	     CMP AL, 00001000B
	     JE ZIGZAG
	     
	     CMP AL, 00010000B
	     JE DOMINO
	      
	      JMP OFF
	      
	TOGGLE:
	       MOV DX, PORTB
	       MOV AL, 11111111B
	       OUT DX, AL
	       
		 JMP CHECK_INPUT
		 
	 SNAKE:
		; 1 -> 2   2-> 3    3->4   4->8   8->7     7->6    6->5    5-> 4    4->1
		 MOV DX, PORTB
		 MOV AL, 00000011B
		 OUT DX, AL
		 CALL DELAY
		 CALL CHECK_OFF
		 
		 MOV DX, PORTB
		 MOV AL, 00000110B
		 OUT DX, AL
		 CALL DELAY
		 CALL CHECK_OFF
		 
		 MOV DX,PORTB
		 MOV AL, 00001100B
		 OUT DX, AL
		 CALL DELAY
		 CALL CHECK_OFF
		 
		 MOV DX, PORTB
		 MOV AL, 10001000B
		 OUT DX, AL
		 CALL DELAY
		 CALL CHECK_OFF
		 
		 MOV DX, PORTB
		 MOV AL, 11000000B
		 OUT DX, AL
		 CALL DELAY
		 CALL CHECK_OFF
		 
		 MOV DX, PORTB
		 MOV AL, 01100000B
		 OUT DX, AL
		 CALL DELAY
		 CALL CHECK_OFF
		 
		 MOV DX, PORTB
		 MOV AL, 00110000B
		 OUT DX, AL
		 CALL DELAY
		 CALL CHECK_OFF
		 
		 MOV DX, PORTB
		 MOV AL, 00010001B
		 OUT DX, AL
		 CALL DELAY
		 CALL CHECK_OFF
		 
		 
		  JMP CHECK_INPUT
		  
		  
	 CYCLONE:
		      ; 2 3- >  1 4 -> 5 8 -> 6 7  
		 MOV DX, PORTB
		 MOV AL, 00000110B
		 OUT DX, AL
		 CALL DELAY
		 CALL CHECK_OFF
		 
		 MOV DX, PORTB
		 MOV AL, 00001001B
		 OUT DX,AL
		 CALL DELAY
		 CALL CHECK_OFF
		 
		 MOV DX, PORTB
		 MOV AL, 10010000B
		 OUT DX,AL
		 CALL DELAY
		 CALL CHECK_OFF
		 
		 MOV DX, PORTB
		 MOV AL, 01100000B
		 OUT DX, AL
		 CALL DELAY
		 CALL CHECK_OFF
		 
	 ZIGZAG:
		  ;1,6,3,8,4,7,2,5
		  
		  MOV DX, PORTB
		  MOV AL, 00000001B
		  OUT DX, AL
		  CALL DELAY
		  CALL CHECK_OFF
		  
		  MOV DX, PORTB
		  MOV AL, 00100000B
		  OUT DX, AL
		  CALL DELAY
		  CALL CHECK_OFF
		  
		  MOV DX, PORTB
		  MOV AL, 00000100B
		  OUT DX, AL
		  CALL DELAY
		  CALL CHECK_OFF
		  
		  MOV DX, PORTB
		  MOV AL, 10000000B
		  OUT DX, AL
		  CALL DELAY
		  CALL CHECK_OFF
		  
		  MOV DX, PORTB
		  MOV AL, 00001000B
		  OUT DX, AL
		  CALL DELAY
		  CALL CHECK_OFF
		  
		  MOV DX, PORTB
		  MOV AL, 01000000B
		  OUT DX, AL
		  CALL DELAY
		  CALL CHECK_OFF
		  
		  MOV DX, PORTB
		  MOV AL, 00000010B
		  OUT DX, AL
		  CALL DELAY
		  CALL CHECK_OFF
		  
		  MOV DX, PORTB
		  MOV AL, 00010000B
		  OUT DX, AL
		  CALL DELAY
		  CALL CHECK_OFF
		  
		  JMP CHECK_INPUT
		  
	 DOMINO: 
		  ;1234
		  ;5678
		  
		  ;1-5 , 5-2, 6-5, 6-2, 6-3, 6-7, 7-3, 7-4, 7-8, 8-4
		  ; 8-3, 8-7, 7-3, 7-2, 7-6, 6-2, 6-1, 6-5, 
		  
		  MOV DX, PORTB
		  MOV AL, 00010001B
		  OUT DX, AL
		  CALL DELAY
		  CALL CHECK_OFF
		  
		  MOV DX, PORTB
		  MOV AL, 00010010B
		  OUT DX, AL
		  CALL DELAY
		  CALL CHECK_OFF
		  
		  MOV DX, PORTB
		  MOV AL, 00110000B
		  OUT DX,AL
		  CALL DELAY
		  CALL CHECK_OFF
		  
		  MOV DX, PORTB
		  MOV AL, 00100010B
		  OUT DX, AL
		  CALL DELAY
		  CALL CHECK_OFF
		  
		  MOV DX, PORTB
		  MOV AL, 00100100B
		  OUT DX, AL
		  CALL DELAY
		  CALL CHECK_OFF
		  
		  MOV DX, PORTB
		  MOV AL, 01100000B
		  OUT DX, AL 
		  CALL DELAY
		  CALL CHECK_OFF
		  
		  MOV DX, PORTB
		  MOV AL, 01000100B
		  OUT DX, AL 
		  CALL DELAY
		  CALL CHECK_OFF
		  
		  MOV DX, PORTB
		  MOV AL, 01001000B
		  OUT DX, AL
		  CALL DELAY
		  CALL CHECK_OFF
		  
		  MOV DX, PORTB
		  MOV AL, 11000000B
		  OUT DX, AL
		  CALL DELAY
		  CALL CHECK_OFF
		  
		  MOV DX, PORTB
		  MOV AL, 10001000B
		  OUT DX, AL
		  CALL DELAY
		  CALL CHECK_OFF
		  
		  MOV DX, PORTB
		  MOV AL, 10000100B
		  OUT DX, AL
		  CALL DELAY
		  CALL CHECK_OFF
		  
		  MOV DX, PORTB
		  MOV AL, 11000000B
		  OUT DX, AL
		  CALL DELAY
		  CALL CHECK_OFF
		  
		  MOV DX, PORTB
		  MOV AL, 01000100B
		  OUT DX, AL
		  CALL DELAY
		  CALL CHECK_OFF
		  
		  MOV DX, PORTB
		  MOV AL, 01000010B
		  OUT DX, AL
		  CALL DELAY
		  CALL CHECK_OFF
		  
		  MOV DX, PORTB
		  MOV AL, 01100000B
		  OUT DX, AL
		  CALL DELAY
		  CALL CHECK_OFF
		  
		  MOV DX, PORTB
		  MOV AL, 00100010B
		  OUT DX, AL
		  CALL DELAY
		  CALL CHECK_OFF
		  
		  MOV DX, PORTB
		  MOV AL, 00100001B
		  OUT DX, AL
		  CALL DELAY
		  CALL CHECK_OFF
		  
		  MOV DX, PORTB
		  MOV AL, 00110000B
		  OUT DX, AL
		  CALL DELAY
		  CALL CHECK_OFF
		  
		  JMP CHECK_INPUT
		  
	 OFF:
		  MOV DX,PORTB
		  MOV AL, 0H
		  OUT DX,AL
		  CALL DELAY
		  
		  
		  JMP CHECK_INPUT
		  
	   CHECK_OFF:
		   MOV DX, PORTA
		   IN AL, DX
		   
		   CMP AL, 00H
		   JE OFF
		   
		   ret
		 
		 
	 DELAY:
	    MOV BX, 0AAAAH
	     L1:
		  DEC BX
		  NOP
		  JNZ L1
		  RET
ENDLESS:
        JMP ENDLESS
CODE    ENDS
        END START